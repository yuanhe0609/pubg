server:
  port: 8085
  encoding:
    charset: UTF-8
    force: true
spring:
  application:
    name: pubg-user-data
  messages:
    encoding: UTF-8
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
